---
const { name, img } = Astro.props;
---

<div class='flex flex-col justify-center items-center gap-3'>
  <img
    src={`./${img}.svg`}
    alt='logo'
    class='w-24'
  />
  <h3>{name}</h3>
</div>
